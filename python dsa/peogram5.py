# Travesal
# bs array ko loop karna
arr = [3,4,5,6,7]
for i in range(len(arr)):
    print(arr[i])

#use : sum, max, count, min, len

#Two pointer technique
# ek pointer start se aur ek pointer end se
arr = [3,4,5,6,7]
start = 0
end = len(arr) - 1

#use : reverse array, check palindrome, find pair with given sum, merge two sorted arrays

#Hashing
# ek data structure jisme key-value pair store hota hai
#use : count frequency of elements, check if element is present, find unique elements, group anagrams, 
# find pairs with given sum, implement a cache, implement a set, implement a dictionary  
s = set(arr)  
#use: find unique elements, check if element is present, implement a set and duplicacy

#sliding window
#use : continious subarray
